'use client'

import { Button } from '@/shared/ui/button'

export function WriteButton() {
  return (
    <Button variant="default" size="default" className="cursor-pointer rounded-2xl px-6">
      글 쓰기
    </Button>
  )
}
